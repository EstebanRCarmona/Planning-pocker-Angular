{
  "version": 2,
  "buildCommand": "ng build --configuration production && npm --prefix server install && npm --prefix server run build",
  "outputDirectory": "server/dist",
  "env": {
    "NODE_ENV": "production"
  },
  "regions": ["iad1"]
}
